import React from 'react';
import CurrencyConversion from '@/pages/CurrencyConversion';

// Currency conversion doesn't need user-specific data
const AdminCurrencyConversion: React.FC = () => {
  return <CurrencyConversion />;
};

export default AdminCurrencyConversion;
